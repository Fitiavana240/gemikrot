# Mot de passe du Postgres lancé par docker-compose. À reporter à
# l'identique dans DATABASE_URL (apps/backend/.env), sans quoi le backend ne
# se connectera pas. Postgres ne le fixe qu'à la création du volume : le
# changer ici après coup n'affecte pas une base déjà initialisée.
POSTGRES_PASSWORD=""
